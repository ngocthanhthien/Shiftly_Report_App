{
  "name": "shiftly-report-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "deploy": "npm test && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "6.2.4",
    "jsdom": "^30.0.1",
    "wrangler": "4.131.1"
  }
}
